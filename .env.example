# Unleash MCP Server Configuration

# Required: Your Unleash instance base URL.
# Both forms are accepted — the MCP server normalizes a trailing /api away if
# present, so you can paste the same value most Unleash SDKs use.
#   Preferred: https://app.unleash-hosted.com/your-instance
#   Also OK:   https://app.unleash-hosted.com/your-instance/api
#   Localhost: http://localhost:4242
UNLEASH_BASE_URL=

# Required: Personal Access Token (PAT) for authentication
# Generate this from your Unleash instance: Profile -> Personal Access Tokens
# Ensure the token has permissions to create feature flags in your projects
UNLEASH_PAT=

# Optional: Default project ID to use when not specified in tool calls
# This allows you to omit the projectId parameter in create_flag
# Example: default, my-project, etc.
UNLEASH_DEFAULT_PROJECT=

# Optional: Default environment (reserved for future use)
# Not currently used but available for future functionality
UNLEASH_DEFAULT_ENVIRONMENT=development

# Optional: Logging configuration
# LOG_LEVEL accepts debug, info, warn, error (default is error when unset)
LOG_LEVEL=
# APP_LOG_FILE writes application logs to a file (falls back to stderr when unset)
APP_LOG_FILE=
# MCP_STDIO_LOG_FILE tees MCP stdio to a file for debugging (requires absolute path)
MCP_STDIO_LOG_FILE=
